var stars = findAllStars();
bindClickListener(stars);
restoreStars(stars);

function findAllStars(){
    let stars = document.querySelectorAll('.star-js img');
    return stars;
}

function bindClickListener(stars) {

}

function restoreStars(stars) {

}